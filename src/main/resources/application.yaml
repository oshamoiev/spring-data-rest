spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test-db
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: update
#    show-sql: true
#    properties:
#      hibernate:
#        format_sql: true
